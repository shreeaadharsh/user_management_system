{
  "name": "user_management_system",
  "version": "1.0.0",
  "description": "Full stack user management system deployment",
  "scripts": {
    "build": "npm install --include=dev --prefix backend && npm install --include=dev --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/scripts/seed.js && node backend/server.js"
  }
}
